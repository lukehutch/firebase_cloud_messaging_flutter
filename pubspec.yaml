name: firebase_cloud_messaging_flutter
description: Send Firebase Cloud Message Messages directly from your flutter or
  Dart Application.

version: 1.0.6

homepage: https://github.com/OttomanDeveloper/firebase_cloud_messaging_flutter

environment:
  sdk: ">=2.12.0 <4.0.0"

dependencies:
  collection: ^1.19.0
  googleapis_auth: ^1.6.0
  http: ^1.2.1
  json_annotation: ^4.9.0
  json_serializable: ^6.8.0

dev_dependencies:
  build_runner: ^2.4.11
  lints: ^4.0.0
  test: ^1.25.8
